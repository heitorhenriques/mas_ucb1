// this file is create by JaCaMo before executing an application
// it includes all packages declared in the JCM as a gradle dependency

apply plugin: 'java'

repositories {
  // This is where Gradle will search for libraries
  mavenCentral()
}

dependencies {
  // Add JFreeChart dependency with desired version
  implementation 'org.jfree:jfreechart:1.5.4' // Replace with the latest version if needed
}
